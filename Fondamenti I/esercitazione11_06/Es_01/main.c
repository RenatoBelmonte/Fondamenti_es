#include "unici.h"

int main(void)
{
    int v[8] = {2, 4, 5, 4, 5, 5, 7, 9};
    size_t size = 8;
    size_t new_size;

    int *res = malloc(size*sizeof(int));
    res = unici(v, size, &new_size);

    img->data[r*img->cols+c];


    return 0;
}
